import { modulesSharedTypes } from './modules-shared-types'

describe('modulesSharedTypes', () => {
  it('should work', () => {
    expect(modulesSharedTypes()).toEqual('modules-shared-types')
  })
})
