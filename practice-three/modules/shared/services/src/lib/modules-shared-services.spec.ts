import { modulesSharedServices } from './modules-shared-services'

describe('modulesSharedServices', () => {
  it('should work', () => {
    expect(modulesSharedServices()).toEqual('modules-shared-services')
  })
})
