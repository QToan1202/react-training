import { modulesSharedStore } from './modules-shared-store'

describe('modulesSharedStore', () => {
  it('should work', () => {
    expect(modulesSharedStore()).toEqual('modules-shared-store')
  })
})
